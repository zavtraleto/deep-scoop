# MEMORY DIVE

2D exploration extraction arcade, вид сбоку. Существо ныряет в лабиринт памяти, стирает ковшом объекты, набирает груз и возвращается на базу, пока его не поймали. Веб, прототип на десктопе, целевая платформа — мобильный браузер.

**Источник истины — GDD:** https://claude.ai/artifact/6S9Gsz7tvwzBZHDnThur9V (читать через Artifact `action: "read"`). Перед каждым новым этапом перечитывай его: пользователь его обновляет. Ссылайся на разделы как `§5.3`.

## Как работаем

- Разработка по этапам из §18. Каждый этап заканчивается играбельной сборкой. Не забегай на следующий этап без согласия пользователя.
- Этап начинается с разбивки на подзадачи. Вопросы по дизайну задавай сразу, не додумывай молча. Если GDD допускает два прочтения, выбери одно, сделай параметром в панели тюнинга и явно сообщи.
- Ощущения (управление, стирание) оценивает пользователь. Твоя задача — дать измеримое поведение, тесты и ползунки для тюнинга.
- Общение, комментарии в коде и названия в панели тюнинга — на русском. Идентификаторы — на английском.

## Команды

```bash
npm run dev        # Vite на :5173 (--host, доступен с телефона в той же сети)
npm test           # vitest run
npm run typecheck  # tsc --noEmit
npm run build      # typecheck + production build
```

Перед тем как сказать «готово»: `npm run typecheck` и `npm test` зелёные, изменение проверено в браузере.

## Репозиторий и деплой

- GitHub: `zavtraleto/deep-scoop`, ветка `main`. Коммитить и пушить только по просьбе пользователя. Автор коммитов задан локально в репо.
- Каждый пуш в `main` запускает `.github/workflows/deploy.yml`: `npm ci` → `npm test` → `npm run build` → GitHub Pages. Упавшие тесты блокируют выкладку.
- Игра: https://zavtraleto.github.io/deep-scoop/ — пользователь играет с телефона. `vite.config.ts` использует `base: './'`, поэтому пути к ассетам только относительные.
- `gh` лежит в `C:\Program Files\GitHub CLI\gh.exe`, если его нет в PATH сессии.

## Стек

TypeScript (strict), three.js, Vite, vitest, lil-gui (панель тюнинга). Сохранение — localStorage.

## Структура `src/`

Папки по фичам, один уровень вложенности. Логика и её отрисовка лежат в папке фичи. `render/` — только общая инфраструктура отрисовки.

| Папка | Что внутри |
|---|---|
| `main.ts` | Точка входа. В dev кладёт `window.game` и `window.config` для отладки |
| `core/` | `game.ts` — сцена, цикл с фиксированным шагом, связывание систем. `config.ts` — все настраиваемые параметры (Приложение A) |
| `math/` | `vec2.ts` — Vec2, clamp, damp, angleDelta, отрезки |
| `input/` | Ввод на Pointer Events: тач — виртуальный стик, мышь — «держи ЛКМ и веди курсором», WASD для отладки. `stickMath.ts` — чистая математика стика и курсора |
| `player/` | `movement.ts` — физика движения (чистая функция), `playerSprite.ts`, `cargoGauge.ts` (камера груза в теле, §14), `stickZonesDebug.ts` |
| `collect/` | Сбор и парящие объекты (§6–7, §11). Логика: `eraseMask.ts` (маска, заливка выпуклых многоугольников), `scoop.ts`, `memoryObject.ts` (объект/кусок = тело + маска-вырезка корня), `collector.ts` (стирание, груз, шум), `fracture.ts` (связные области, раскол, крошки), `floatPhysics.ts` (парение, разлёт, стены, толчки), `collectWorld.ts` (всё вместе + события для отрисовки). `artLibrary.ts` (картинки: размер по равной площади, обрезка по альфе, выбор по месту). Отрисовка: `objectArt.ts` (загрузка картинок из `assets/objects/`, заглушки, сборка объекта из картинки), `memoryObjectView.ts` (шейдер стирания), `scoopView.ts`, `eraseParticles.ts`. `testHelpers.ts` — только для тестов |
| `world/` | `grid.ts` — сетка клеток, срез углов, отрезки стен. `collision.ts`, `worldMesh.ts` |
| `gen/` | Генерация карты (§4). `layout.ts` — модель раскладки чанка (слоты, залы, ходы, острова, враги), `roomShape.ts` — форма зала (прямоугольник или «капля»), `geometry.ts` — геометрия ходов с изломом, `validate.ts` — правила §4.4 и мосты по Тарьяну, `buildChunk.ts` — раскладка → сетка + расстановка, `handLayouts.ts` — ручные карты, `layoutDebug.ts` — отладочный слой |
| `echo/` | Видимость (§8–9). Логика: `light.ts` (форма света, многоугольник видимости с тенями), `visibility.ts` (состояния клеток), `echoPulse.ts` (таймер, кольца), `sight.ts` (что видит игрок, слепки памяти). Отрисовка: `lightMaskView.ts` (маска света и эха в текстуру экрана), `memoryView.ts` (слой памяти + размытие), `fogView.ts` (туман и память поверх мира), `lightGlowView.ts`, `echoView.ts` (кольца и заряд у игрока) |
| `minimap/` | Карта во весь экран. `mapKnowledge.ts` — логика: развёртка сквозь стены, открытые контуры, точки предметов и врагов. `minimapView.ts` — отрисовка своей ортокамерой поверх кадра |
| `enemies/` | Враги (§10 + спецификация пользователя). Логика: `enemy.ts` (состояние, знание об игроке, `targetFns` — выбор цели по типу), `navGrid.ts` (BFS по клеткам с запасной «ближайшей доступной», сетка с закрытой базой, `segmentClear`), `enemySystem.ts` (события обнаружения, путь, движение с инерцией, поимка). Отрисовка: `enemyView.ts` (процедурные фигуры, маркеры эха), `enemyDebug.ts` |
| `assets/objects/` | Картинки Memory Objects пользователя: `small/`, `medium/`, `large/` — класс задаётся папкой |
| `render/` | `followCamera.ts` (включая `screenToWorld`); позже фон, пост-обработка (§15) |
| `debug/` | `fpsMeter.ts` — счётчик кадров слева сверху (средний fps и худший кадр за 0.5 с). `tuning.ts` — lil-gui. Клавиша G прячет всю отладку: панель и отладочные элементы сцены (`debugConfig.visible`) |

Будущие этапы — новые папки того же уровня: `meta/` (база, валюта, прокачка, сохранение, §12), `ui/` (HUD, §14).

## Архитектурные правила

- **Логика отделена от three.js.** Физика, генерация, ИИ врагов — чистые функции/классы без импорта three, покрытые тестами. Three.js только в классах отрисовки.
- **Мир 2D.** Координаты: x вправо, y вверх, глубина — это −y. Клетка `(cx, cy)`, cy растёт вниз: `x = cx·CELL`, `y = −cy·CELL`. `CELL = 2`, радиус игрока 0.5.
- **Фиксированный шаг физики** 1/120 с в `Game.fixedUpdate`, отрисовка интерполирует. Новые системы симуляции обновляй там же, не в `frame`.
- **Все числа геймдизайна — в `core/config.ts`** с комментарием и единицами. Каждый параметр, влияющий на ощущения, выведи в `debug/tuning.ts`. Меняется набор параметров → подними версию `STORAGE_KEY`, иначе старые сохранения из localStorage перетрут значения.
- **Стены** — отрезки из `Grid.buildWalls()`, коллизия круг-отрезок со скольжением. Углы 45° — диагональные клетки из `Grid.cutCorners()`. Срез делается, только если обе стены угла продолжаются: так на ступеньках не бывает торчащих зубцов.

## Модель движения (решения пользователя)

`player/movement.ts`, «заднеприводная машина», намеренно отличается от буквы §5.2:
- у существа есть нос (`heading`), спрайт смотрит носом; тяга идёт по носу и слабеет, пока нос не смотрит на стик;
- круг стика делится **относительно носа** и поворачивается вместе с ним: задняя четверть (90°) прямо позади носа — торможение (на малой скорости — разворот на месте), остальные три четверти — зона поворота, нос смотрит в её середину. Отладочный круг зон рисует `player/stickZonesDebug.ts`;
- на большой скорости при резком повороте сцепление срывается: нос перекручивается внутрь поворота, тело скользит;
- у носа есть инерция вращения (`turnVel`): поворот раскручивается и тормозит заранее, не проскакивая цель. Пользователь хочет «вес» и ощущение плотной среды, резкий разворот на месте отвергнут. На малой скорости тяга и ось сцепления направлены к стику, а не по носу, иначе медленный разворот носа даёт дугу;
- груз (§5.3) замедляет разгон и торможение, ослабляет доворот и сцепление; торможение без ввода от массы не зависит.

Пользователь прямо отверг два варианта: «стоп и разгон без заноса» и «разворот огромной дугой с набором скорости». Тесты в `movement.test.ts` фиксируют это поведение на боевых значениях конфига.

## Карта (решения этапа 3)

- **Крупнее и природнее, чем в GDD («нора»):** слот 12×12 клеток, залы 5–10, ходы шириной 2–4, чанк 4×6 слотов = 48×72 клетки. Залы смещены в слотах, поэтому ходы разной длины. Базой служит весь ряд 0.
- **Залы:** `rect` или `blob`. Blob — это основной эллипс плюс 1–2 «лопасти» с шумом по углу, сглаженные клеточным автоматом. Форма детерминирована по id. Внутри залов бывают острова и колонны (решение пользователя), объекты в таких залах ставятся явным смещением в стороне.
- **Ходы:** прямые по перекрытию сторон или с Z-изломом (`jog`), между одной парой залов может быть два параллельных хода. Конец хода «докапывается» до пола неровного зала. Узкие зазубрины засыпает `Grid.fillNotches`.
- **Клетки остаются 2×2 со срезами 45°** — пользователь отложил гладкие контуры и прочие «странности» до поздних этапов генерации.
- Генерация живёт в `gen/` и будет развиваться: кастомные комнаты, случайный генератор §4.5 (этап 7). Ручная карта — это раскладка (`handLayouts.ts`), а не клетки. Её растеризует тот же `buildChunk`, а тест прогоняет через `validateLayout`.
- Этап 3 делаем по пунктам и после каждого останавливаемся на тюнинг с пользователем: 1) генерация, 2) видимость (свет, эхо), 3) туман-память, 4) карта (сделана после пункта 1 этапа 4), 5) картинки объектов.

## Бэклог

- **Большие уровни** — при росте карты до сотен тысяч клеток: меши и текстура изведанности по чанкам, компактный индекс стен, усыпление дальних объектов, подгрузка чанков. Решать на этапе 7, когда ясен целевой размер.
- **Производительность на мобильных** — уровни качества и автоподстройка разрешения внутренних слоёв (память, свечение). Решать на этапе 8 вместе с визуалом.

## Видимость (решения этапа 3)

- **Свет игрока:** тёплый «подводный» жёлтый, два слоя — тусклый ореол вокруг существа (2 клетки) и более яркий луч-прожектор ~60° на 4 клетки по носу, мягкие края. Стены отбрасывают тени (многоугольник видимости по отрезкам стен). Маска света: R — ореол, G — прожектор, B — свечение за фронтом эха; `fogView` рассеивает туман по ним с разной силой, `lightGlowView` добавляет тёплый подсвет. Свет отмечает клетки Explored 20 раз в секунду. Клетку стены «видно», когда видна её грань.
- **Эхо:** радиус **100 ед.** (решение пользователя; в Приложении A — 8 клеток), остальное по §9 и Приложению A. Дальние лучи видимости эха идут обходом клеток (`marchRay`), а не перебором всех стен — импульс стоит ~0.3 мс. Эхо **сквозь стены не проходит** (решение пользователя, отличается от §16): при импульсе строится круговой многоугольник видимости, кольцо рисуется и открывает клетки только в нём. За угол волна не заходит. Свечение после волны считается **попиксельно** (расстояние до точки импульса и время с прохода фронта) — поклеточное свечение пользователь отверг как «ячеистое». По клеткам хранится только изведанность. Кольцо заряда вокруг игрока — HUD.
- **Осколков (§8) в игре нет** — пользователь убрал их вместе со всем кодом. Импульс идёт строго по таймеру.
- **Порядок отрисовки:** мир и объекты → туман (`renderOrder` 10) → кольца эха → маркеры и враги (18–19, видимость решает `EnemyView`) → ковш, игрок, частицы (20+) → отладка (30).
- **Визуал пока простой** (решение пользователя): однотонный пол без сетки, эхо без налёта и «хвоста», тонкое кольцо. Цвет скалы равен цвету тумана — иначе вокруг изведанных стен появляется светлый ореол. Над визуалом работаем на этапе 8.
- **«Мутная память»** (решение пользователя): Explored вне света и эха показывает не живой мир, а слой памяти. Слой памяти — это слой сцены `MEMORY_LAYER` (пол и стены плюс слепки объектов), он рисуется в текстуру в половинном разрешении и размывается. Туман выцветляет, затемняет и слегка «плывёт» его. Unknown — темнота.
- **Слепки** (`sight.ts`, `Recollection`): видна вещь — слепок переезжает к ней; видно место слепка, а вещи там нет — слепок стирается. У каждого вида объекта есть `ghost` в слое памяти, живые меши — только в слое 0.
- Размытие делается несколькими проходами с шагом не больше ~1.5 px: при большем шаге Гаусс даёт полосы-двойники.

## Управление мышью (решение пользователя)

- Тач и перо — виртуальный стик, как в §5.1. Мышь — отдельная схема: **пока зажата ЛКМ**, персонаж плывёт к курсору. Без кнопки мышь персонажем не управляет.
- Цель — точка под курсором, пересчитанная каждый шаг с текущей камерой (как в agar.io). Это не фиксированная точка мира: камера едет следом, и курсор в стороне даёт бесконечное движение.
- Вектор от персонажа к курсору переводится в обычный вектор ввода (`cursorToInput`): мёртвая зона 0.6 ед., полная сила на 5 ед. Дальше работает та же модель движения, включая задний сектор-тормоз.
- Системный курсор над полем скрыт, вместо него кольцо (при зажатой ЛКМ ярче и меньше). Линии к персонажу нет — так решил пользователь.
- Курсор ушёл с поля или на панель тюнинга, окно потеряло фокус — ввод гаснет. Галка `mouseConfig.asStick` возвращает старый режим «мышь как стик».

## Сбор (решения этапа 2)

- **Ковш смотрит по скорости, а не по носу** (решено с пользователем). Полоса всегда во всю ширину, а занос становится инструментом стирания. На малой скорости ковш плавно переходит на нос. Галка `followNose` оставлена для сравнения.
- Полоса за шаг — выпуклая оболочка прошлого и текущего переднего края плюс сам прямоугольник ковша: разрывов нет на любой скорости.
- Маска: строка 0 — низ (как UV). У `DataTexture` маски `unpackAlignment = 1`, ширина не кратна 4. Картинка объекта читается в разрешение маски с переворотом строк.
- Шум (§6.4) и прогресс считаются только по непрозрачным пикселям.
- Состояние забега (текущий груз) в localStorage не сохраняется: живёт в `Game.run`.

## Карта (решения пользователя, вместо мини-карты §9.4)

- **Во весь экран, всегда поверх игры** — «двойная экспозиция»: тонкие белые контуры открытых стен, аддитивно и полупрозрачно. Своя ортокамера охватывает в `scale` (5) раз больше мира и стоит так, что **персонаж на карте всегда 1 в 1 совпадает с персонажем в игре** (центр карты C = P − s·(P − G), P — игрок, G — центр игрового экрана). У персонажа карта прозрачнее, к краям ярче (`clearRadius` → `fullRadius`).
- **Контуры — только то, что игрок видел сам:** светом или игровым эхом (оно сквозь стены не идёт). Источник — `VisibilityMap.fresh`, контур клетки открывается в момент, когда клетка стала изведанной. Базу заранее не открываем.
- **Развёртка** — второй фронт того же импульса эха (одно событие): та же скорость, радиус 200 ед., **сквозь стены, но открывает только точки**, не стены. Сам фронт на карте **не рисуется** (решение пользователя). Слышимость пульса врагами — отдельный параметр.
- **Точки, как локатор из «Чужого»:** зелёные — предметы (каждый кусок отдельно), красные — враги. Точка ставится там, где сущность застал фронт, вспыхивает. Точка врага гаснет за `enemyLife` (~ до следующего импульса). Точка предмета горит, пока следующая развёртка не пройдёт по её месту: предмета там нет — стирается, уплыл — переезжает.
- Рисуется после основного кадра (`MinimapView.render`, `autoClear` выключен): контуры, точки (18 px), стрелка игрока. Красные маркеры врагов в игровом мире остаются.

## Враги (этап 4, спецификация пользователя)

Намеренно отличается от §9.1 и §10. **Пульс — информационное событие для ИИ:** игрок, глядя эхом, выдаёт себя.
- Враг не знает, где игрок. Позицию (и направление движения, и расстояние) дают только события:
  - **пульс** — слышен **сквозь стены** в радиусе `pulseHearRadius` (по умолчанию = радиус эха), враг узнаёт, когда до него дошёл фронт волны, позицию — на момент импульса;
  - **шум сбора** — пока ковш стирает, ближние враги узнают позицию раз в `noiseInterval`; при шуме враги быстрее на 25% (§10.3);
  - **прямая видимость** вблизи (`sightRadius`, по настоящим стенам) — видит, пока видит; скрылся — остаётся последняя позиция;
  - груз ≥ 50% умножает все радиусы обнаружения (`cargoDetectScale`).
- Между событиями враг идёт к цели из последнего знания, пришёл — ждёт. Сна и scatter из §10.4 нет.
- **Типы различаются только `targetFns[kind](enemy, info)`**; поиск пути и движение общие. Недостижимая цель → ближайшая доступная клетка. Пункты этапа: 1) общая система + Hunter (к последней позиции), 2) Interceptor (позиция + N клеток по направлению движения, без направления — позиция), 3) Flanker (точка сбоку/позади, сторона случайно при каждом обнаружении, дошёл — меняет сторону), 4) Coward (к игроку дальше SAFE_DISTANCE, ближе — к своей безопасной точке, гистерезис; «кружит на дистанции»).
- Движение — «существо с инерцией»: BFS по клеткам (4 раза в с) только задаёт направление, путь срезается по прямой (`segmentClear` с радиусом тела). Радиус 0.7.
- База закрыта для врагов и путём, и телом: у врагов своя сетка (`enemyGrid`) с залитой базой и свои стены.
- Игрок видит врага только в своём свете (с тенями). Маркер (§9.3, 3 с) ставится, когда фронт эха застал врага **и** враг в поле зрения волны. Вспышка «заметил» — враг белеет и раздувается.
- Поимка: существо сжимается ~0.8 с, затем полный сброс уровня (решение пользователя; экран базы — этап 5). Отладка: «👁 враги» (всегда видны, путь, цель, крест последней позиции: красный — пульс, оранжевый — шум, белый — видит), «бессмертие».
- Враги задаются в раскладке: `enemies: ['hunter']` у зала; в базе запрещены (`validateLayout`).

## Картинки объектов (решения пользователя, этап 3)

- Картинки — **PNG/WebP пользователя** с прозрачным фоном, процедурные рисунки отвергнуты (телевизор удалён). Файл кладётся в `src/assets/objects/<small|medium|large>/`, `import.meta.glob` подхватывает его сам. Картинки грузятся до старта (`loadArtLibrary` в `main.ts`, экран «погружение…»).
- Класс (папка) задаёт размер и ценность. **Размер — по равной площади:** площадь = (size · диаметр игрока)², пропорции из картинки. Прозрачные поля обрезаются, большая сторона ≤ 1024 px.
- Частью предмета считается пиксель с альфой > 96: полупрозрачные тени и ореолы стереть нельзя, их в картинках быть не должно.
- Картинка выбирается детерминированно по месту (`id зала#номер`). В ручной карте можно задать её явно: `{ cls, art: 'имя-файла' }`. Добавление файла в папку может поменять выбор на других местах.
- Класс без картинок — нейтральная плашка с буквой S/M/L. Искажения поверх картинок нет: оно заложено в саму графику.
- Одна текстура на картинку (`Game.artTextures`), её делят все экземпляры и куски.
- Репозиторий публичный: картинки должны быть свои или с подходящей лицензией.

## Парящие объекты и раскол (решения пользователя)

- Объекты не стоят на месте: медленно дрейфуют у своего «дома» по плавной кривой и покачиваются на ±3°, у каждого свои фазы.
- **Раскол по реальному разрезу:** если после стирания нестёртая картинка распалась на несколько связных областей (4-соседство), каждая крупная область становится куском. Выемка без прохода насквозь объект не раскалывает.
- Кусок — это `MemoryObject` с вырезкой маски корня (`offsetX/Y`). Чужие пиксели в вырезке помечены стёртыми. Ценность куска пропорциональна его пикселям, так что ценность объекта сохраняется. Картинка (текстура) одна на все куски корня.
- Куски меньше `minPieceArea` — крошки: сразу рассыпаются частицами в груз.
- Разлёт медленный, как у «разрезанного под водой плода»: перпендикулярно пути игрока, вращение едва заметно (не больше ~4°). Осев, кусок заводит новый дом там, где остановился.
- Мягкие столкновения со стенами (круг равной площади) и расталкивание кусков. Ковш слегка толкает стираемое: лёгкое сильнее, тяжёлое едва. Игрока объекты не тормозят (§6.2).
- В момент раскола края кусков вспыхивают, вдоль разреза гаснут искры, которые не летят в игрока.
- Проверка на раскол идёт раз в `fractureCheckInterval`, пока объект стирают, и сразу, как только стирать перестали.

## Тесты

- vitest, файлы `*.test.ts` рядом с кодом. Тестируй чистую логику: физику, геометрию, генерацию, ИИ.
- Тюнинг физики делай симуляцией в тесте: временный `*.test.ts` с `console.log` метрик (пронос, дуга, время). Смотри вывод через `--reporter=verbose` (иначе console.log не виден), потом удали файл.

## Подводные камни окружения (Windows)

- **Не правь файлы через PowerShell** (`Get-Content`/`Set-Content` в PS 5.1 портят UTF-8 и русский текст). Только Edit/Write. Перемещения — `Move-Item`, это безопасно.
- Вывод vitest/npm проще фильтровать через Bash (`grep`), в PowerShell stderr от npm превращается в ошибки.
- В Browser pane, если панель скрыта, `requestAnimationFrame` не тикает. Для проверки гоняй кадры вручную: `game.frame(t)` с монотонным `t` от `performance.now()`. Ввод эмулируй через `KeyboardEvent` на `window` или `PointerEvent` на `#game`.
- При горячей перезагрузке в консоли копятся ошибки промежуточных состояний. Перед выводами перезагрузи страницу.
